{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git add -A 2>/dev/null; git diff --cached --quiet 2>/dev/null || git commit -m 'chore: checkpoint' 2>/dev/null || true; git push 2>/dev/null || true",
            "timeout": 30
          }
        ]
      }
    ]
  }
}